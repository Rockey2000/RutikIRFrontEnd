import { NewrowDirective } from './newrow.directive';

describe('NewrowDirective', () => {
  it('should create an instance', () => {
    const directive = new NewrowDirective();
    expect(directive).toBeTruthy();
  });
});
